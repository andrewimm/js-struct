{
  "name": "js-struct",
  "version": "1.2.0",
  "description": "C-like structs for accessing byte arrays",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  },
  "scripts": {
    "test": "mocha --reporter dot --ui tdd"
  }
}
